; Loaded after 99-opcache.ini in the production image only - see Dockerfile.
; Code is baked into the image and never changes at runtime, so we can
; safely skip the filesystem stat() check on every request.
opcache.validate_timestamps = 0
